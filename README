The easiest way to build moj is to import the project into Eclipse
(.classpath and .project files are provided). The script package.sh
creates:
- deploy/moj.jar (load this in the Arena)
- deploy/moj.zip (suitable for distribution)
